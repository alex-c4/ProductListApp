import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Lista de productos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
